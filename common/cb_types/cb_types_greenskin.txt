##Greenskin version of the Beastman Conquest CB
orc_county_conquest = {
	name = CB_NAME_ORC_COUNTY_CONQUEST
	war_name = WAR_NAME_ORC_COUNTY_CONQUEST
	sprite = 22
	truce_days = 549 #A year and a half
	hostile_against_others = yes
	is_permanent = yes
	check_all_titles = yes # if permanent, setting this to true will check against all of someones titles, including vassal held titles
	press_claim = yes
	can_ask_to_join_war = no

	can_use_gui = {
		ROOT = {
			prestige = 5
		}
	}

	on_add = {
		ROOT = { prestige = -5 }
		ROOT = {
			character_event = { id = WAAGH.1 days = 1 }
		}
	}

	can_use = {
		ROOT = {
			NOT = { is_liege_or_above = FROM }
		}
	}
	on_attacker_leader_death = { end_war = invalid }

	can_use_title = {
		OR={
			has_global_flag = clean_mordheim
			AND={
				ROOT = {
					capital_scope = { NOT = { region = world_old_world_empire_mordheim } }
				}
				FROM = {
					capital_scope = { NOT = { region = world_old_world_empire_mordheim } }
				}
			}
		}
		NAND = {
			location = { region = world_albion }
			ROOT = {
				has_global_flag = mists_of_albion
				capital_scope = { NOT = { region = world_albion } }
			}
		}
		NOT = { de_jure_liege_or_above = e_the_under_empire }
		NOT = { de_jure_liege_or_above = e_nehekhara } #the title must have at least one county which is NOT nehekharan (if all are, it is dead land, and cannot be occupied).

		ROOT = { NOT = { has_opinion_modifier = { who = FROM modifier = opinion_scared_goblins } } }
		FROM = { NOT = { has_opinion_modifier = { who = ROOT modifier = opinion_scared_goblins } } }
		tier = count
		OR = {
			FROM = {
				has_landed_title = PREV
			}
			holder_scope = {
				is_liege_or_above = FROM
				NOT = { same_realm = ROOT }
			}
		}

		ROOT = {
			religion_group = greenskin_gods_group
			OR = {
				trait = creature_orc
				trait = creature_goblin
				trait = creature_snotling #Maybe???
			}
		}

		NOT = {
			OR = {
				ROOT = {
					has_landed_title = PREV
				}
				holder_scope = {
					is_liege_or_above = ROOT
				}
			}
		}

		location = {
			any_neighbor_province = {
				owner = {
					OR = {
						character = ROOT
						is_liege_or_above = ROOT
					}
				}
			}
		}
	}

	is_valid_title = {
		OR = {
			FROM = {
				has_landed_title = PREV
			}
			holder_scope = {
				is_liege_or_above = FROM
			}
		}
	}


	on_success = {
		any_attacker = {
			character_event = { id = WAAGH.1 days = 1 }
		}
	}
	on_success_title = {
		if={limit={
				ROOT={
					OR={
						culture_group=goblin_group
						culture_group=orc_group
					}
				}
				FROM={
					OR={
						culture_group=goblin_group
						culture_group=orc_group
						culture_group=beastman_group
					}
				}
			}
			custom_tooltip={text = greenskin_warlord_succ_tip}
			FROM = {
				any_realm_province = {
					if = {
						limit = {
							controller = {
								OR={character = ROOT
								top_liege={character = ROOT}
								}
							}
							OR={
								any_neighbor_province = {
									holder_scope = { same_realm = ROOT }
								}
								distance = { who = ROOT value <= 45 } #Greenskin are Discriminate against Each Other
							}
						}
						county={
						any_direct_de_jure_vassal_title = { # take all baronies too
							limit = {
								has_holder = yes
							}
							show_scope_change = no
							usurp_title = ROOT
						}}
						county = {usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion }}
					}
				}
			}
			any_attacker = {
				limit = { character = ROOT }
				participation_scaled_prestige = 125
				participation_scaled_piety = 50
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_piety = 50 }
				hidden_tooltip = { participation_scaled_prestige = 150 }
			}
		}
		else_if={limit={county={location ={region= world_karaz_ankor}}}
			custom_tooltip={text = greenskin_purge_mountains_succ_tip}
			county = { usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion } }
			any_de_jure_vassal_title = { # take all baronies too
				limit = {
					has_holder = yes
					holder_scope = {
						OR = {
							character = FROM
							is_liege_or_above = FROM
							AND = {
								is_liege_or_above = ROOT
								NOT = { culture = ROOT }
								NOT = { religion = ROOT }
							}
						}
					}
				}
				show_scope_change = no
				usurp_title = ROOT
			}
			any_attacker = {
				limit = { character = ROOT }
				participation_scaled_prestige = 150
				participation_scaled_piety = 175
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_piety = 200 }
				hidden_tooltip = { participation_scaled_prestige = 200 }
			}
		}
		else_if={limit={county={location ={region= world_drak_ankor}}}
			custom_tooltip={text = greenskin_purge_mountains_succ_tip}
			county = { usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion } }
			any_de_jure_vassal_title = { # take all baronies too
				limit = {
					has_holder = yes
					holder_scope = {
						OR = {
							character = FROM
							is_liege_or_above = FROM
							AND = {
								is_liege_or_above = ROOT
								NOT = { culture = ROOT }
								NOT = { religion = ROOT }
							}
						}
					}
				}
				show_scope_change = no
				usurp_title = ROOT
			}
			any_attacker = {
				limit = { character = ROOT }
				participation_scaled_prestige = 125
				participation_scaled_piety = 150
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_piety = 200 }
				hidden_tooltip = { participation_scaled_prestige = 200 }
			}
		}
		else_if={limit={ROOT={ai=yes}}
			usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion }

			any_de_jure_vassal_title = { # take all baronies too
				limit = {
					has_holder = yes
					holder_scope = {
						OR = {
							character = FROM
							is_liege_or_above = FROM
							AND = {
								is_liege_or_above = ROOT
								NOT = { culture = ROOT }
								NOT = { religion = ROOT }
							}
						}
					}
				}
				show_scope_change = no
				usurp_title = ROOT
			}
			any_attacker = {
				limit = { character = ROOT }
				participation_scaled_prestige = 150
				participation_scaled_piety = 150
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_piety = 250 }
				hidden_tooltip = { participation_scaled_prestige = 250 }
			}
		}
		else={
			custom_tooltip={text = greenskin_purge_succ_tip}
			FROM = {
				any_realm_province = {
					if = {
						limit = {
							controller = {
								OR={character = ROOT
								top_liege={character = ROOT}
								}
							}
							OR={
								any_neighbor_province = {
									holder_scope = { same_realm = ROOT }
								}
								distance = { who = ROOT value <= 25 }
							}
						}
						county={
						any_direct_de_jure_vassal_title = { # take all baronies too
							limit = {
								has_holder = yes
							}
							show_scope_change = no
							usurp_title = ROOT
						}}
						county = {usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion }}
					}
				}
			}
			any_attacker = {
				limit = { character = ROOT }
				participation_scaled_prestige = 150
				participation_scaled_piety = 150
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_piety = 250 }
				hidden_tooltip = { participation_scaled_prestige = 250 }
			}
		}
	}

	on_fail_title = {
		ROOT = {
			prestige = -100
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 50
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 50 }
		}
	}

	on_reverse_demand = {
		ROOT = {
			prestige = -200
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 250
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 250 }
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}

	ai_will_do = {
		factor = 1

		modifier = {
			factor = 10
			trait = lunatic_3
		}
		modifier = {
			factor = 0.5
			NOT={trait=ambitious}
		}
		modifier = {
			factor = 0.5
			NOT={trait=greedy}
		}
		modifier = {
			factor = 0.8
			NOR={
				trait=wroth
				trait=cruel
			}
		}
		modifier = {
			factor = 0.01
			ROOT = { religion = spider_god }
			FROM = { religion = shadowmen }
		}
		modifier = {
			factor = 0.01
			ROOT = { religion = spider_god }
			FROM = { trait = vampire_mahtmasi_visible }
		}
	}
}

#For the "Declare WAAAGH!" targetted decision
waaagh_invasion = {
	name = CB_NAME_WAAAGH
	war_name = WAR_NAME_WAAAGH
	sprite = 33
	truce_days = 3650
	hostile_against_others = yes
	is_permanent = yes
	is_holy_war = yes
	can_ask_to_join_war = yes
	battle_warscore_mult = 0.1


	# press_claim = yes


	check_de_jure_tier = KING # this scans all de jure kingdoms for the counties which are held by or vassals(or below) of selected character. Only valid if is_permanent = yes
	apply_short_occ_mod = no # Do not apply the 'recently_conquered' modifier to Holdings

	can_use = {
		ROOT = {
			has_opinion_modifier = { who = FROM modifier = preparing_waaagh_invasion_opinion }
			OR = {
				trait = creature_orc
				trait = creature_goblin
				religion_group = greenskin_gods_group
			}
			NOT = { religion_group = FROM }	#Not against other Greenskins
		}
		# FROM = {
			# any_realm_province = {
				# any_neighbor_province = {
					# owner = {
						# OR = {
							# character = ROOT
							# is_liege_or_above = ROOT
						# }
					# }
				# }
			# }
		# }
	}

	can_use_title = {
		OR={
			has_global_flag = clean_mordheim
			AND={
				ROOT = {
					capital_scope = { NOT = { region = world_old_world_empire_mordheim } }
				}
				FROM = {
					capital_scope = { NOT = { region = world_old_world_empire_mordheim } }
				}
			}
		}
		NAND = {
			location = { region = world_albion }
			ROOT = {
				has_global_flag = mists_of_albion
				capital_scope = { NOT = { region = world_albion } }
			}
		}
		NOT = { de_jure_liege_or_above = e_the_under_empire }
		NOT = { de_jure_liege_or_above = e_nehekhara } #the title must have at least one county which is NOT nehekharan (if all are, it is dead land, and cannot be occupied).


	}

	is_valid_title = {
		FROM = {
			any_realm_title = {
				de_jure_liege_or_above = PREVPREV
			}
		}
	}

	on_add = {
		ROOT = {
			remove_character_modifier = preparing_waaagh_invasion
			clr_character_flag = preparing_waaagh_invasion_flag
		}
		ROOT = {
		#	usurp_title = { target = PREV type = invasion }
			character_event = { id = WAAGH.1 days = 1 }
		}
	}

	on_success = {
		ROOT = {
			character_event = { id = WAAGH.1 days = 1 }
			add_character_modifier = {
				modifier = attempted_waaagh
				duration = 3650
			}
			# occupy_minors_of_occupied_settlements = FROM
			# gain_all_occupied_titles = { who = FROM type = invasion }
			prestige = 1000
			religion_authority = {
				modifier = won_viking_invasion
			}
		}

		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 500
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 500 }
		}
		FROM = {
			prestige = -500
		}
	}


	on_success_title = {
		custom_tooltip = {
			text = waaagh_succ_tip
			hidden_tooltip = {
				FROM = {
					remove_opinion = {
						who = FROM
						modifier = preparing_waaagh_invasion_opinion_against_me
					}
				}
				# any_de_jure_vassal_title = {
						# limit = {
							# has_holder = yes
							# holder_scope = {
								# OR = {
									# character = FROM
									# is_liege_or_above = FROM
								# }
							# }
						# }
						# holder_scope = {
							# death = {
								# death_reason = death_execution
								# killer = ROOT
							# }
						# }
						# any_de_jure_vassal_title = {
								# holder_scope = {
									# death = {
										# death_reason = death_execution
										# killer = ROOT
									# }
								# }
						# }
				# }
				ROOT = {
					gain_settlements_under_title = {
						title = PREV
						enemy = FROM
						type = holy_war
					}
					any_realm_province = {
						limit = {
							de_jure_liege_or_above = PREVPREV
							OR = {
								NOT = { culture = ROOT }
								NOT = { religion = ROOT }
							}
						}
						culture = ROOT
						religion = ROOT
						any_province_holding = {
							limit = {
								is_capital = yes
							}
							convert_to = TRIBAL
							add_building = tb_greenskin_camp_1
							refill_holding_levy = yes
						}
						any_province_holding = {
							limit = {
								is_capital = no
							}
							destroy_settlement = THIS
						}
					}
					any_demesne_title = {
						limit = { tier = BARON }
						remove_holding_modifier = recently_conquered
						refill_holding_levy = yes
					}
					clr_character_flag = preparing_waaagh_invasion_flag
					remove_opinion = {
						who = FROM
						modifier = preparing_waaagh_invasion_opinion
					}
					# Settle the special invasion forces
					disband_event_forces = prepared_invasion
				}
			}
		}
	}

	on_fail = {
		FROM = {
			prestige = 200
			set_character_flag = imperial_hero
			remove_opinion = {
						who = FROM
						modifier = preparing_waaagh_invasion_opinion_against_me
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 200
			set_character_flag = imperial_hero
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 200 }
			set_character_flag = imperial_hero
		}
		ROOT = {
				add_character_modifier = {
					modifier = attempted_waaagh
					duration = 3650
				}
				clr_character_flag = preparing_waaagh_invasion_flag
				remove_opinion = {
					who = FROM
					modifier = preparing_waaagh_invasion_opinion
				}
			prestige = -500
			hidden_tooltip = {
				disband_event_forces = prepared_invasion
			}
			religion_authority = {
				modifier = lost_viking_invasion
			}
		}
	}

	on_fail_title = { ROOT = { remove_claim = PREV } }

	on_reverse_demand = {
		ROOT = {
				add_character_modifier = {
					modifier = attempted_waaagh
					duration = 3650
					}
					clr_character_flag = preparing_waaagh_invasion_flag
					remove_opinion = {
						who = FROM
						modifier = preparing_waaagh_invasion_opinion
					}
			prestige = -1000
			transfer_scaled_wealth = {
				to = FROM
				value = 2.0
			}
			hidden_tooltip = {
				disband_event_forces = prepared_invasion
			}
			religion_authority = {
				modifier = lost_viking_invasion
			}
		}
		FROM = {
			prestige = 500
			set_character_flag = imperial_hero
			remove_opinion = {
						who = FROM
						modifier = preparing_waaagh_invasion_opinion_against_me
			}
		}
		any_defender = {
			limit = { character = FROM }
			set_character_flag = imperial_hero
			participation_scaled_prestige = 250
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			set_character_flag = imperial_hero
			hidden_tooltip = { participation_scaled_prestige = 250 }
		}
	}

	# on_reverse_demand_title = { ROOT = { remove_claim = PREV } }

	on_attacker_leader_death = { end_war = invalid }

	attacker_ai_victory_worth = {
		factor = 200
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
	ai_will_do = {
		factor = 1

		modifier = {
			factor = 0
			NOT = {
					ROOT = {
						had_character_flag = {
							flag = preparing_waaagh_invasion_flag
							days = 200
						}
					}
			}
		}
		modifier = {
			factor = 100
			ROOT = {
				had_character_flag = {
					flag = preparing_waaagh_invasion_flag
					days = 200
				}
			}
		}
	}
}

greenskin_uprising = {
	name = CB_NAME_GREENSKIN_UPRISING
	war_name = WAR_NAME_GREENSKIN_UPRISING
	sprite = 16
	truce_days = 365
	hostile_against_others = yes
	is_permanent = yes
	check_all_titles = yes # if permanent, setting this to true will check against all of someones titles, including vassal held titles
	#press_claim = yes

	attacker_can_call_allies = no
	defender_can_call_allies = no
	can_ask_to_join_war = no

	is_independence = yes

	can_use = {
		ROOT = {
			primary_title = { temporary = yes } # Revolter or adventurer trigger
			war = no
			NOT = { is_liege_or_above = FROM }
		}
	}

	# on_attacker_leader_death = { if = { limit = { ROOT = { trait = morghur } }
					# end_war = invalid }
				# }

	can_use_title = {
		tier = count
		NOT = { de_jure_liege_or_above = e_the_under_empire }
		NOT = { de_jure_liege_or_above = e_nehekhara } #the title must have at least one county which is NOT nehekharan (if all are, it is dead land, and cannot be occupied).

	}

	is_valid_title = {
		OR = {
			FROM = {
				has_landed_title = PREV
			}
			holder_scope = {
				is_liege_or_above = FROM
			}
		}
	}

	on_success_title = {
		occupy_minors_of_occupied_settlements = FROM
		gain_all_occupied_titles = { who = FROM type = invasion }
		usurp_title_plus_barony_if_unlanded = { target = ROOT type = revolt }
		ROOT = {
			set_defacto_liege = ROOT 
			prestige=500
			piety=125
		}
		FROM = {
			prestige = -100
			piety = -100
		}
	}

	on_fail_title = {
		FROM = {
			prestige = 10
			hidden_tooltip = {
				opinion = {
					modifier = opinion_savage_invader
					who = ROOT
					months = 1200
				}
			}
		}
		ROOT = {
			death = {
				death_reason = death_execution
				killer = FROM
			}
		}
	}

	on_reverse_demand = {
		FROM = {
			prestige = 20
			hidden_tooltip = {
				opinion = {
					modifier = opinion_savage_invader
					who = ROOT
					months = 1200
				}
				add_character_modifier = {
					name = scattered_greenskins
					days = 1825
				}
			}
		}
		ROOT = {
			death = {
				death_reason = death_execution
				killer = FROM
			}
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}
}

purge_greenskins = {
	name = CB_NAME_ORC_COUNTY_PURGE
	war_name = WAR_NAME_ORC_COUNTY_PURGE
	sprite = 17
	truce_days = 549 #A year and a half
	hostile_against_others = yes
	is_permanent = yes
	check_all_titles = yes # if permanent, setting this to true will check against all of someones titles, including vassal held titles
	press_claim = yes
	can_ask_to_join_war = no
	infamy_modifier = 2.0
	capturing_defender_is_complete_victory = no

	can_use_gui = {
		ROOT = {
			wealth = 20
			conditional_tooltip = {
				trigger = {
					from={
						OR={
							religion=spider_god
							culture=goblin_forest
						}
					}
				}
				prestige = 250
				wealth=40
			}
		}
	}

	on_add = {
		if={limit={ #Forest Goblins are Notoriusly hard to get rid of, 1 county at time
				from={
					OR={
						religion=spider_god
						culture=goblin_forest
					}
				}
			}
			ROOT = { wealth = -20 }
			ROOT = { prestige = -250 }
		}
		
		ROOT = { wealth = -20 }
	}


	can_use = {
		ROOT = {
			NOT = { trait = vampire_necrarch_visible }
			NOT = { trait = creature_nehekharan }
			NOT = { trait = creature_beastman }
			NOT = {
				OR = { culture_group = lizardman_group
					religion_group = greenskin_gods_group
					culture_group = orc_group
					culture_group = goblin_group
				}
			}
		}
		FROM = {
			OR = {
				religion_group = greenskin_gods_group
				culture_group = orc_group
				culture_group = goblin_group
			}
		}

	}

	can_use_title = {
		OR={
			location = {
				NOR={
					region = world_karaz_ankor
					region = world_drak_ankor
					terrain = mountain
					terrain = impassable_mountains
				}
			}
			ROOT={NOT={religion=dwarf_gods_group}}
		}
		OR={
			has_global_flag = clean_mordheim
			AND={
				ROOT = {
					capital_scope = { NOT = { region = world_old_world_empire_mordheim } }
				}
				FROM = {
					capital_scope = { NOT = { region = world_old_world_empire_mordheim } }
				}
			}
		}
		NAND = {
			location = { region = world_albion }
			ROOT = {
				has_global_flag = mists_of_albion
				capital_scope = { NOT = { region = world_albion } }
			}
		}
		NOT = { de_jure_liege_or_above = e_the_under_empire }
		tier = count
            OR = { 	ROOT = { ai = no }
			 NOT = { ROOT = { trait = creature_dwarf } }
		  AND = { capital_scope = { region = world_karaz_ankor }
			 capital_scope = { region = world_drak_ankor } }
		 }
            OR = { 	ROOT = { ai = no }
			ROOT = { OR = {religion_group = chaos_gods_group
					religion_group = old_gods_group }}
		  	NOT = { capital_scope = { region = world_chaos_wastes } }
		 }
		OR = {
			FROM = {
				has_landed_title = PREV
			}
			holder_scope = {
				is_liege_or_above = FROM
				NOT = { same_realm = ROOT }
			}
		}


		NOT = {
			OR = {
				ROOT = {
					has_landed_title = PREV
				}
				holder_scope = {
					is_liege_or_above = ROOT
				}
			}
		}

		location = {
			any_neighbor_province = {
				owner = {
					OR = {
						character = ROOT
						is_liege_or_above = ROOT
					}
				}
			}
		}
	}

	is_valid = {
		FROM = {
			OR = {
				trait = creature_orc
				trait = creature_goblin
				trait = creature_snotling
			}
		}
	}

	is_valid_title = {
		OR = {
			FROM = {
				has_landed_title = PREV
			}
			holder_scope = {
				is_liege_or_above = FROM
			}
		}
	}

	on_success  = {
	}

	on_success_title = {
		if={limit={county={location ={region= world_karaz_ankor}}}
			custom_tooltip={text = greenskin_purge_mountains_succ_tip}
			county = { usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion } }
			any_de_jure_vassal_title = { # take all baronies too
				limit = {
					has_holder = yes
					holder_scope = {
						OR = {
							character = FROM
							is_liege_or_above = FROM
							AND = {
								is_liege_or_above = ROOT
								NOT = { culture = ROOT }
								NOT = { religion = ROOT }
							}
						}
					}
				}
				show_scope_change = no
				usurp_title = ROOT
			}
			any_attacker = {
				limit = { character = ROOT }
				participation_scaled_prestige = 100
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_prestige = 100 }
			}
			add_province_modifier = {
				name = greenskin_population_bad
				duration = -1
			}
		}
		else_if={
			limit={
				from={
					culture = goblin_forest
					religion = spider_god
				}
			}
			custom_tooltip={text = greenskin_purge_mountains_succ_tip}
			custom_tooltip={text = greenskin_spiderkult_succ_tip}
			county = { usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion } }
			any_de_jure_vassal_title = { # take all baronies too
				limit = {
					has_holder = yes
					holder_scope = {
						OR = {
							character = FROM
							is_liege_or_above = FROM
							AND = {
								is_liege_or_above = ROOT
								NOT = { culture = ROOT }
								NOT = { religion = ROOT }
							}
						}
					}
				}
				show_scope_change = no
				usurp_title = ROOT
			}
			any_attacker = {
				limit = { character = ROOT }
				participation_scaled_prestige = 100
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_prestige = 100 }
			}
			add_province_modifier = {
				name = greenskin_population_bad
				duration = -1
			}
		}
		else_if={limit={county={location ={region=world_drak_ankor}}}
			custom_tooltip={text = greenskin_purge_mountains_succ_tip}
			county = { usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion } }
			any_de_jure_vassal_title = { # take all baronies too
				limit = {
					has_holder = yes
					holder_scope = {
						OR = {
							character = FROM
							is_liege_or_above = FROM
							AND = {
								is_liege_or_above = ROOT
								NOT = { culture = ROOT }
								NOT = { religion = ROOT }
							}
						}
					}
				}
				show_scope_change = no
				usurp_title = ROOT
			}
			any_attacker = {
				limit = { character = ROOT }
				participation_scaled_prestige = 100
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_prestige = 100 }
			}
			add_province_modifier = {
				name = greenskin_population_bad
				duration = -1
			}
		}
		else_if={limit={ROOT={ai=yes}}
			usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion }

			any_de_jure_vassal_title = { # take all baronies too
				limit = {
					has_holder = yes
					holder_scope = {
						OR = {
							character = FROM
							is_liege_or_above = FROM
							AND = {
								is_liege_or_above = ROOT
								NOT = { culture = ROOT }
								NOT = { religion = ROOT }
							}
						}
					}
				}
				show_scope_change = no
				usurp_title = ROOT
			}
			FROM = {
				any_realm_province = {
					if = {
						limit = {
							controller = {
								OR={character = ROOT
								top_liege={character = ROOT}
								}
							}
							OR={
								any_neighbor_province = {
									holder_scope = { same_realm = ROOT }
								}
								distance = { who = ROOT value <= 25 }
							}
						}
						add_province_modifier = {
							name = greenskin_population_bad
							duration = -1
						}
						county={
						any_direct_de_jure_vassal_title = { # take all baronies too
							limit = {
								has_holder = yes
							}
							show_scope_change = no
							usurp_title = ROOT
						}}
						county = {usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion }}
					}
				}
			}
			add_province_modifier = {
				name = greenskin_population_bad
				duration = -1
			}
		}
		else={
		custom_tooltip={text = greenskin_purge_succ_tip
        FROM = {
			any_realm_province = {
				if = {
					limit = {
						controller = {
								OR={character = ROOT
								top_liege={character = ROOT}
								}
						}
						OR={
							any_neighbor_province = {
								holder_scope = { same_realm = ROOT }
							}
							distance = { who = ROOT value <= 20 }
						}
					}
					add_province_modifier = {
						name = greenskin_population_bad
						duration = -1
					}
					county={
					any_direct_de_jure_vassal_title = { # take all baronies too
						limit = {
							has_holder = yes
						}
						show_scope_change = no
						usurp_title = ROOT
					}}
					county = {usurp_title_plus_barony_if_unlanded = { target = ROOT type = invasion }}
				}
			}
		}
		any_attacker = {
			limit = { character = ROOT }
			participation_scaled_prestige = 100
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
		}}
	}

	on_fail_title = {
		ROOT = {
			prestige = -100
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 50
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 50 }
		}
	}

	on_reverse_demand = {
		ROOT = {
			prestige = -200
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_prestige = 100
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 100 }
		}
	}

	attacker_ai_victory_worth = {
		factor = 100 # No, Why would i give up when Am winning
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}

	ai_will_do = {
		factor = 1

		modifier = {
			factor = 0	#Have dwarves focus on the mountains
			NOT = { FROM = { de_jure_liege = e_karak_ankor } }
			ROOT = { trait = creature_dwarf }
		}
		modifier = {
			factor = 0	#Have dwarves focus on the mountains
			NOT = { FROM = { top_liege = { de_jure_liege = e_karak_ankor } } }
			ROOT = { trait = creature_dwarf }
		}
		modifier = {
			factor = 0.01
			FROM = { religion = spider_god }
			ROOT = { religion = shadowmen }
		}
		modifier = {
			factor = 0
			FROM = { religion = spider_god }
			ROOT = { trait = vampire_mahtmasi_visible }
		}
	}
}
####New

# Migrating
greenskin_migration = {
	name = CB_NAME_GREENSKIN_MIGRATION
	war_name = WAR_NAME_GREENSKIN_MIGRATION
	display_on_map = no
	sprite = 30
	truce_days = 650
	is_permanent = yes
	is_holy_war = yes
	check_de_jure_tier = DUKE # this scans all dejure duchies for the counties which are held by or vassals(or below) of selected character. Only valid if is_permanent = yes

	can_use_gui = {
		ROOT = {
			prestige = 500
			is_within_diplo_range = FROM
		}
		ROOT={
			OR={
				has_wonder = no
				ai = no
			}
		}
		custom_tooltip = {
			text = greenskin_minor_migration
			hidden_tooltip = {
				ROOT={
					OR={
						not={has_character_flag = minor_migration }
						had_character_flag = {
							flag = minor_migration
							years >= 25
						}
					}
				}
			}
		}
	}

	on_add = {
		ROOT = { prestige = -500 }
		clr_character_flag = minor_migration
		ROOT = {
		#	usurp_title = { target = PREV type = invasion }
			character_event = { id = WAAGH.1 days = 1 }
		}
	}

	can_use = {
		ROOT = {
			OR={
				trait=creature_orc
				trait=creature_goblin
			}
			#independent = yes
			higher_tier_than = BARON
			OR={
				lower_tier_than = KING
				AND={
					government=greenskin_waagh_government
					NOT = {
						ROOT = { primary_title = { num_of_realm_counties = { value = 5 title = PREV } } }
					}
				}
			}
		}
		FROM = {
			OR={
				trait=creature_orc
				trait=creature_goblin
			}
		}
	}
	on_attacker_leader_death = { end_war = invalid }

	can_use_title = {
		OR={
			has_global_flag = clean_mordheim
			AND={
				ROOT = {
					capital_scope = { NOT = { region = world_old_world_empire_mordheim } }
				}
				FROM = {
					capital_scope = { NOT = { region = world_old_world_empire_mordheim } }
				}
			}
		}
		NAND = {
			location = { region = world_albion }
			ROOT = {
				has_global_flag = mists_of_albion
				capital_scope = { NOT = { region = world_albion } }
			}
		}
		NOT = { de_jure_liege_or_above = e_the_under_empire }
		NOT = { de_jure_liege_or_above = e_nehekhara } #the title must have at least one county which is NOT nehekharan (if all are, it is dead land, and cannot be occupied).

		NOT = { capital_scope = { distance = { who = ROOT distance = 400 } } }

	}

	is_valid = {
		ROOT = {higher_tier_than = BARON}
	}

	on_success = {
		root ={set_character_flag = minor_migration}
		any_attacker = {
			character_event = { id = WAAGH.1 days = 1 }
			limit = { character = ROOT }
			participation_scaled_piety = 500
			participation_scaled_prestige = 750
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = {
				character_event = { id = WAAGH.1 days = 1 }
				participation_scaled_piety = 500
				participation_scaled_prestige = 1000
			}
		}

		ROOT = { any_demesne_title = { limit = { NOT = { is_primary_type_title = yes }
							NOT = { higher_tier_than = DUKE } }
					set_title_flag = migrating_away }
			any_vassal = { set_character_flag = migrating_away_vassal }
			occupy_minors_of_occupied_settlements = FROM
			gain_all_occupied_titles = { who = FROM type = invasion }

				capital_holding = {
					add_holding_modifier = {
						modifier = nomad_population_boom
						years = 8
						stacking = yes
					}
				}
			population = 1000
			}
		FROM = { piety = -50 }
		FROM = { prestige = -150 }
	}

	on_success_title = {
		random_direct_de_jure_vassal_title = { # take all baronies too
			limit = {
				owner = { same_realm = FROM }
				tier = count
			}
			usurp_title = ROOT
		}
		custom_tooltip = {
			text = tribal_invasion_succ_tip
			hidden_tooltip = {
				ROOT = {
					vassalize_or_take_under_title_destroy_duchies = {
						title = PREV
						enemy = FROM
						is_crusade = yes # Even if the title holder is not participating in the war, gain holdings occupied by all Crusade participants
						type = invasion
					}
				}
			}
		}
		any_de_jure_vassal_title = { # take all baronies too
			limit = { owner = { character = FROM } }
			usurp_title = ROOT
		}
		hidden_tooltip = {
			ROOT = {
			#	usurp_title = { target = PREV type = invasion }
				character_event = { id = beastmen.10 days = 1 }
			}
		}
	}

	on_fail = {
		ROOT = {
			piety = -50
			prestige = -200
		}
	}

	on_reverse_demand = {
		ROOT = {
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			piety = -75
			prestige = -150
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_piety = 200
			participation_scaled_prestige = 100
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = {
				participation_scaled_piety = 200
				participation_scaled_prestige = 100
			}
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}

	ai_will_do = {
		factor = 1

		modifier = {
			factor = 10
			capital_scope = { has_province_modifier = province_scourged }
		}
		modifier = {
			factor = 0.25
			FROM = { trait = creature_goblin }
		}
	}
}

# 61: Greenskins can subjugate Weaker&Smaller Greenskins
greenskin_subjugation = {
	name = CB_NAME_GREENSKIN_SUBJUGATION
	war_name = WAR_NAME_GREENSKIN_SUBJUGATION
	sprite = 32
	truce_days = 365
	is_permanent = yes
	can_ask_to_join_war = yes
	allowed_to_target_tributaries = no
	defender_unoccupied_warscore = yes
	max_attacker_occupation_score=85
	sort_priority = 1100

	can_use_gui = {
		ROOT={piety=250}
		#higher_tier_than between ROOT and FROM would be better but its tooltip doesn't say anything about comparing character tiers
		FROM = {
			is_nomadic = no
			NOT = {tier = emperor}
			NOT={higher_tier_than=ROOT}
		}
		AND={
			OR={
				FROM={
					relative_realm_size = {
						who = ROOT
						size < 2.0
					}
					lower_tier_than=KING
				}
				FROM={
					relative_realm_size = {
						who = ROOT
						size < 3.0
					}
					higher_tier_than=DUKE
				}
			}
			ROOT = {relative_power = { who = FROM power > 2.0 }}
		}
		FROM = {}
	}

	can_use = {
		#FROM={
		#	relative_realm_size = {
		#		who = ROOT
		#		size < 2.0
		#	}
		#}
		#ROOT = {
		#	relative_power = { who = FROM power < 1.2 }
		#}
		FROM = {
			religion_group = greenskin_gods_group
			is_nomadic = no # Nomads cannot be subjugated
			in_revolt = no
			NOT = {
				any_war = {
					defender = { character = PREVPREV }
					OR = {
						using_cb = nomad_subjugation
						using_cb = greenskin_subjugation
						using_cb = pagan_subjugation
					}
				}
			}
			NOT = { is_offmap_governor = yes }
			any_neighbor_independent_ruler = {
				character=ROOT
			}
			independent=yes
		}
		ROOT = {
			independent=yes
			OR = {
				ai = yes
				multiplayer = no
				NOT = {
					has_game_rule = {
						name = multiplayer_invasions
						value = off
					}
				}
			}
			religion_group = greenskin_gods_group
			NOT = { same_realm = FROM }
			NOT = { is_liege_or_above = FROM }
			mercenary = no
			OR = {
				NOT = { has_character_modifier = launched_subjugation }
				has_opinion_modifier = {
					modifier = declared_independence_nomad
					who = FROM
				}
			}
		}
		OR = {
			ROOT = {
				ai = no
			}
			ROOT = {
				any_realm_title = {
					tier = COUNT
					count = 8
				}
			}
		}
	}

	on_add = {
		ROOT={piety=-125}
		hidden_tooltip = { fire_haruspicy_event_effect = yes }
		if = {
			limit = { defender = { is_offmap_governor = offmap_cathay } }
			attacker = {
				sound_effect = china_angered_emperor
				detract_grace_super_huge_effect = yes
			}
		}
	}

	on_success = {
		hidden_tooltip = {
			ROOT = {
				if = {
					limit = {
						has_nickname = no
					}
					FROM = {
						save_event_target_as = nickname_target
					}
					ROOT = {
						save_event_target_as = nickname_receiver
					}
					random_list = {
						2 = { give_nickname = nick_the_eastern_wind }
						5 = { give_nickname = nick_the_despoiler }
						5 = {
							give_nickname = nick_the_bane_of_realmname
							modifier = {
								factor = 0
								event_target:nickname_target = { is_nomadic = yes }
							}
							modifier = {
								factor = 0
								event_target:nickname_target = { culture = ROOT }
							}
						}
						5 = {
							give_nickname = nick_the_culture_marauder
							modifier = {
								factor = 0
								event_target:nickname_target = { is_nomadic = yes }
							}
							modifier = {
								factor = 0
								event_target:nickname_target = { culture = ROOT }
							}
						}
						100 = { }
					}
				}
			}
		}
		if={limit={from={tier=count}}
			ROOT = {
				participation_scaled_prestige = 250
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_piety = 50 }
				hidden_tooltip = { participation_scaled_prestige = 250 }
			}
		}
		if={limit={from={tier=duke}}
			ROOT = {
				participation_scaled_prestige = 500
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_piety = 100 }
				hidden_tooltip = { participation_scaled_prestige = 500 }
			}
		}
		if={limit={from={tier=king}}
			ROOT = {
				participation_scaled_piety = 250
				participation_scaled_prestige = 750
			}
			any_attacker = {
				limit = { NOT = { character = ROOT } }
				hidden_tooltip = { participation_scaled_piety = 250 }
				hidden_tooltip = { participation_scaled_prestige = 750 }
			}
		}
		FROM = {
			piety = -100
			prestige = -100

			if = {
				limit = {
					is_reformed_religion = ROOT
					controls_religion = no
				}
				religion = ROOT # Reformed pagans are forced to convert back
			}

			custom_tooltip = {
				text = tribal_subjugation_tip
			}

			hidden_tooltip = {
				opinion = {
					modifier = opinion_subjugated
					who = ROOT
				}
				any_vassal = {
					opinion = {
						modifier = opinion_subjugated
						who = ROOT
					}
				}

				any_demesne_title = {
					limit = {
						higher_tier_than = ROOT
						NOT = { controls_religion = yes }
					}
					ROOT = { grant_title_no_opinion = { target = PREV type = invasion } }
					add_pressed_claim = PREV
				}

				any_demesne_title = {
					limit = {
						tier = ROOT
						NOT = { controls_religion = yes }
					}
					ROOT = { grant_title_no_opinion = { target = PREV type = invasion } }
					add_pressed_claim = PREV
				}

				if = {
					limit = {
						NOT = {
							any_demesne_title = {
								controls_religion = yes
							}
						}
					}
					set_defacto_liege = ROOT
					break = yes
				}
				any_vassal = {
					set_defacto_liege = ROOT
				}
				any_demesne_title = {
					limit = {
						NOT = { controls_religion = yes }
					}
					ROOT = {
						grant_title_no_opinion = { target = PREV type = invasion }
					}
				}
			}
		}
		hidden_tooltip = {
			check_if_reconquista_finished_effect = yes
			FROM = {
				landless_pope_effect = yes
			}
		}
	}

	on_fail = {
		FROM = {
			piety = 125
			participation_scaled_prestige = 250
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 250 }
		}
		ROOT = {
			piety = -250
			prestige = -250
		}
	}

	on_invalidation = {
		if = {
			limit = {
				NOT = {
					has_opinion_modifier = {
						modifier = declared_independence_nomad
						who = FROM
					}
				}
			}
			ROOT = {
				remove_character_modifier = launched_subjugation
			}
		}
	}

	on_reverse_demand = {
		ROOT = {
			piety = -500
			prestige = -500
			transfer_scaled_wealth = {
				to = FROM
				value = 2.0
			}
		}
		FROM = {
			piety = 250
			participation_scaled_prestige = 500
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = { participation_scaled_prestige = 500 }
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}

	ai_will_do = {
		factor = 10
		modifier = {
			factor = 0.5
			ROOT = {
				demesne_size = 1
			}
		}
	}
}
###Waagh
waaagh_war = {
	name = CB_NAME_GREENSKIN_WAAAGH
	war_name = WAR_NAME_GREENSKIN_WAAAGH
	display_on_map = no
	sprite = 33
	truce_days = 7
	is_permanent = yes
	check_all_titles=yes
	#check_de_jure_tier = EMPIRE
	is_holy_war = yes
	can_ask_to_join_war = yes
	full_hostility = yes
	
	allowed_to_target_tributaries = no
	max_attacker_battle_score = 50
	max_defender_battle_score = 125
	max_attacker_occupation_score = 25
	max_defender_occupation_score = 25
	allow_distant = yes # AI parameter
	capturing_defender_is_complete_victory = NO
	battle_warscore_mult = 0.1
	infamy_modifier = 10
	sort_priority = 1

	can_use_gui = {
		ROOT = {
			prestige = 50
			population_factor >= 0.25
			is_within_diplo_range = FROM
			NOT = { has_character_modifier = attempted_waaagh }
		}
		FROM={
			and={
				tier=king
				num_of_count_titles_in_realm < 8
			}
			and={
				tier=emperor
				num_of_count_titles_in_realm < 15
			}
		}
	}

	on_add = {
		ROOT = { prestige = -50 }
		clr_character_flag = minor_migration
		ROOT = {
			character_event = { id = WAAGH.1 days = 1 }
			set_character_flag=using_waaagh_cb
		}
		clr_character_flag=preparing_waaagh_invasion_flag
		any_playable_ruler = {
			limit={not={character=from}}
			character_event = { id = DR_greenskins.174 days = 12 random = 30 }		# Warn Everyone
			hidden_effect ={
				if={limit={religion_group=FROM}
					opinion = {
						who = ROOT
						modifier = opinion_furious
						months = 60
					}
				}
				if={limit={culture_group=FROM}
					opinion = {
						who = ROOT
						modifier = opinion_furious
						months = 60
					}
				}
			}
		}
		FROM={
			character_event = { id = DR_greenskins.174 days = 1	}
			opinion = {
				who = ROOT
				modifier = preparing_waaagh_invasion_opinion_against_me
				months = 120
			}
		}
	}

	can_use = {
		ROOT = {
			government = greenskin_waagh_government
			independent=yes
			NOT={has_character_flag=using_waaagh_cb}
			OR={
				ai=no
				NOT={has_character_flag=preparing_waaagh_invasion_flag}
			}
		}
		FROM = {
			OR={
				AND={
					tier=emperor
					is_nomadic=no
				}
				population=50000 #Bash in Chaos Wastes
				realm_size >= 100
			}
			NOR={ #For Now?
				trait=creature_orc
				trait=creature_goblin
			}
		}
	}
	on_attacker_leader_death = { end_war = invalid }

	can_use_title = {
		FROM={
			primary_title={
				title=PREVPREV
			}
		}
		NAND = {
			location = { region = world_albion }
			ROOT = {
				has_global_flag = mists_of_albion
				capital_scope = { NOT = { region = world_albion } }
			}
		}
		NOT = { capital_scope = { distance = { who = ROOT distance = 700 } } }

	}

	is_valid = {
		ROOT = {
			higher_tier_than = BARON	
			government=greenskin_waagh_government
			any_war = {
				using_cb = waaagh_war
				war_score >= -50
			}
		}
	}
	on_invalidation = {
		ROOT = {
			if={
				limit={
					NOT = {
						any_owned_bloodline = {
							has_bloodline_flag = waaagh_warlord_bloodline_flag
						}
					}
				}
				create_bloodline = {
					type = waaagh_warlord_lesser_bloodline
				}
			}
			current_heir={
				add_character_modifier = {
					modifier = attempted_waaagh
					duration = 1825
				}
			}
			death = {
				death_reason = death_execution
				killer = FROM
			}
		}
	}
	on_success = {
		any_attacker = {
			character_event = { id = WAAGH.1 days = 1 }
			limit = { character = ROOT }
			participation_scaled_piety = 150
			participation_scaled_prestige = 250
		}
		any_attacker = {
			limit = { NOT = { character = ROOT } }
			hidden_tooltip = {
				participation_scaled_piety = 150
				participation_scaled_prestige = 250
			}
		}

		ROOT = {
			if={
				limit={
					NOT = {
						any_owned_bloodline = {
							has_bloodline_flag = waaagh_greater_warlord_bloodline_flag
						}
					}
				}
				create_bloodline = {
					type = waaagh_warlord_bloodline
				}
			}
			clr_character_flag=using_waaagh_cb
			add_character_modifier = {
				modifier = attempted_waaagh
				duration = 365
			}

			capital_holding = {
				add_holding_modifier = {
					modifier = nomad_population_boom
					years = 10
					stacking = yes
				}
			}
			population = 5000
			piety=1000
			prestige=1000
		}
		FROM = { 
			piety = -50 
			transfer_scaled_wealth = {
				to = ROOT
				value = 2.5
			}
		}
		FROM = { prestige = -150 }
	}

	on_success_title = {
	}

	on_fail = {
		ROOT = {
			clr_character_flag=using_waaagh_cb
			piety = -50
			prestige = -200
			add_character_modifier = {
				modifier = attempted_waaagh
				duration = 1065
			}
		}
	}

	on_reverse_demand = {
		ROOT = {
			transfer_scaled_wealth = {
				to = FROM
				value = 4.0
			}
			piety = -75
			prestige = -150
		}
		ROOT = {
			current_heir={
				add_character_modifier = {
					modifier = attempted_waaagh
					duration = 3650
				}
			}
			death = {
				death_reason = death_execution
				killer = FROM
			}
		}
		any_defender = {
			limit = { character = FROM }
			participation_scaled_piety = 200
			participation_scaled_prestige = 100
		}
		any_defender = {
			limit = { NOT = { character = FROM } }
			hidden_tooltip = {
				participation_scaled_piety = 200
				participation_scaled_prestige = 100
			}
		}
	}

	attacker_ai_victory_worth = {
		factor = -1 # always accept
	}

	attacker_ai_defeat_worth = {
		factor = 100
	}

	defender_ai_victory_worth = {
		factor = -1 # always accept
	}

	defender_ai_defeat_worth = {
		factor = 100
	}

	ai_will_do = {
		factor = 1
	}
}